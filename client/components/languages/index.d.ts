export declare const Languages: {
    data(): {
        languages: {
            text: string;
            code: string;
        }[];
    };
    template: string;
    methods: {
        chooseLanguage(code: string): void;
    };
};
